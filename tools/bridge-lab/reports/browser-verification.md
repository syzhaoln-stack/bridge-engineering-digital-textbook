# Web V1 浏览器验收

日期：2026-09-09。Godot 4.7 stable 单线程 Web 导出；发布 PCK SHA-256：`6d9f403b81bb310895703f4e9d2f74d6d789d8ff2d6b6c8cb68703bf596e944c`。

## 已核验

- 从公开教材第1章入口进入预览，再点击进入 Godot 程序，链接正常。1440×980 预览无横向溢出，不请求 WASM、PCK 或 GLB。
- 普通程序入口不提供 QA 接口，选择桥型前不下载 GLB。测试入口的清单顺序为梁、拱、斜拉、悬索。
- 在公开 `docs` 副本提供的 HTTP 站点上，桌面 Chrome 逐一加载四桥及标准背景，正常交互没有页面或控制台错误。
- T梁全部2424构件显示，新增河道、岸坡与接引路对齐。背景加载、关闭释放、施工初始阶段、恢复成桥状态均通过。
- 其余三桥测试了施工首阶段、主梁组消隐、切换阶段仍保持消隐、恢复、爆炸和背景。爆炸时教学受力曲线自动关闭。切桥清除旧背景、选择、隔离、隐藏和选取三角缓存。
- 模型请求返回503时显示失败状态，解除故障并重试后恢复全部2424个T梁构件。
- 手机竖屏、横屏、转屏、触控及截图验证另见 `mobile-browser-qa.md`。

| 桥型 | 导入构件 | 成桥可见 | 初始施工阶段可见 | 关闭主梁组后可见 |
|---|---:|---:|---:|---:|
| 梁 | 2424 | 2424 | 32 | — |
| 拱 | 1843 | 1771 | 92 | 1351 |
| 斜拉 | 1782 | 1678 | 42 | 733 |
| 悬索 | 4749 | 4381 | 42 | 3391 |

成桥可见数小于导入数，是施工临时设施已撤除；并非丢失模型构件。

## 压缩下载回归

正式 CDN 压缩响应暴露了浏览器与 HTTPRequest 重复解压的问题，已在统一请求入口关闭 Web 分支的二次解压。原生分支继续接受 gzip。修复前，同一 gzip 测试服务器返回 HTTP200，但引擎清单解析失败；修复后禁用缓存，清单、四主体、四背景、四份受力数据共13个 gzip 响应均由实际浏览器引擎成功读取。桌面另复验 T梁两档背景、真实按钮开关、爆炸、施工首阶段32构件及复位，控制台错误0。模型、材质与触摸实现未改动。

## 验证边界

Windows 上 Playwright WebKit 26.5 的 iPhone 视口模拟可启动并渲染拱桥1843构件，采用无 MSAA 分支。控制台仍出现 `glBlitFramebuffer: Read and write color attachments cannot be the same image` 警告，因此不能宣称 WebKit 完全无错误或已经通过 iPhone 真机验收。没有物理 Android/iOS 设备的帧率、峰值内存、温升、浏览器后台恢复数据；桌面模拟帧率不作为手机性能承诺。

发布文件体积为原始字节，浏览器实际传输量还受服务器压缩与缓存影响。四主体合计30,838,780 B；包括引擎、所有可选背景档及说明的完整运行目录约99.31 MB。单桥按需加载，完整运行目录不会一次下载。
